package com.asadjawaid.taskmanagement.util;

public enum TaskStatus {
    NEW, // Represents a newly created task that has not been started
    IN_PROGRESS, // Represents a task that is actively being worked on or in progress
    COMPLETED, // Represents a task that has been finished or completed
    CLOSED // Represents a task that has been closed or not being worked on
}
